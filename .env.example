# Maker — Grok / xAI (signal proposals, risk overlay)
XAI_API_KEY=
XAI_MODEL=grok-4.20-0309-non-reasoning

# Checker — Claude / Anthropic (independent verifier; maker ≠ checker)
ANTHROPIC_API_KEY=
# alias supported:
# CLAUDE_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-20250514

# TradingView BTCUSDT alert webhook (leading feed tier)
TRADINGVIEW_WEBHOOK_SECRET=
TRADINGVIEW_HOST=0.0.0.0
TRADINGVIEW_PORT=8799
TRADINGVIEW_SIGNALS_PATH=reports/tradingview_signals.jsonl

# Leading CEX feeds (Binance + Coinbase) — evaluated before Chainlink
CEX_MAX_STALENESS_MS=5000

# Chainlink settlement truth (lagging oracle — not a leading signal)
ETH_RPC_URLS=https://ethereum-rpc.publicnode.com,https://1rpc.io/eth
CHAINLINK_MAX_AGE_MS=120000

# Read-only monitoring dashboard (paper training run)
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8800
DASHBOARD_PUBLIC_HOST=45.32.224.147
DASHBOARD_TOKEN=

# Optional notifications
SLACK_WEBHOOK_URL=

# Paper trading only — live routing is structurally disabled in grok_bot/safety.py
PAPER_ONLY=true